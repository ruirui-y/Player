# 信令服务器实施笔记

> 基于 DemandStation Boost 复用，2026-08-06 实施。

## 步骤 1: 搭骨架

### 删除清单
```
Services/paymentservice/     ← 支付服务
Services/gateway/             ← 旧业务（TCP服务器/设备管理/支付）
Libs/Proto/                   ← protobuf 协议
Libs/DB/ConnectionPool.*     ← MySQL 连接池
Libs/DB/RedisHelper.*        ← Redis 客户端
Notes/                        ← 旧笔记
Tests/loadtest/               ← Python 压测
Tests/reports/                ← 压测报告
bash/demandstation.sql        ← 旧数据库
```

### 修改清单

**CMakeLists.txt** — project 改名 + 去 Proto
```cmake
// project(DemandStationServer...) → project(SignalingServer...)
// 删除: include_directories(Proto生成目录)
// 删除: add_subdirectory(gateway)
```

**CMakePresets.json** — 生成器改为 Ninja
```json
// Visual Studio 17 2022 → Ninja（单配置生成器, CMAKE_BUILD_TYPE 生效）
// binaryDir: build → build/release (隔离)
// displayName: DemandStation → SignalingServer
```

**Libs/Global/Global.h** — 去 MySQL/Redis/支付
```cpp
// 删除: MYSQL_HOST, MYSQL_PORT, REDIS_HOST, WX_NOTIFY_URL 等
// 新增: ws_port_(8081), stun_port_(3478), db_path_("./signaling.db")
// server_config.json 结构: {"Server":{...}, "Database":{"Path":"..."}}
```

**Libs/Global/Global.cpp** — 精简解析
```cpp
// 删: 解析 Mysql/Redis/WechatPay 节点 (~50行)
// 改: 解析 Server 节点 → http_port_/ws_port_/stun_port_/worker_threads_
// 新: 解析 Database 节点 → db_path_
```

**Libs/Thread/WorkerThread.h** — 去 Redis
```cpp
// 删: class RedisHelper; GetRedisHelper(); FlushRedisCallbacks(); redis_helper_
// 删: extern RedisHelper* GetCurrentThreadRedisHelper();
```

**Libs/Thread/WorkerThread.cpp** — 去 Redis 初始化 + 加 SqlExec Init
```cpp
// 删: RedisHelper 初始化/清理代码
// 删: FlushRedisCallbacks() 实现
// 新: sql_exec_->Init(GlobalConfig::Instance()->GetDbPath());
// 新: GetCurrentThreadSqlExec() 按需创建（非 Worker 线程自动打开 SQLite）
```

**Libs/Thread/ThreadPool.h/.cpp** — 去 Redis
```cpp
// 删: class RedisHelper; RedisGet/Setex/Del/Exists/PipelineGet
// 删: 旧 SqlExec 包装器 Query/Update/Transaction（API 已变）
```

**Libs/DB/CMakeLists.txt** — MySQL→SQLite
```cmake
# 删: find_package(mysqlcppconn), find_package(hiredis)
# 新: find_package(unofficial-sqlite3 CONFIG)
# 新: target_link → unofficial::sqlite3::sqlite3
```

**Libs/DB/SqlExec.h** — MySQL→SQLite 重写
```cpp
// 类: SqlExec { sqlite3* db_; Init(路径); Execute→int; Query→SqlResult; }
// SqlRow: map<string,string>, SqlResult: vector<SqlRow>
```

**Libs/DB/SqlExec.cpp** — SQLite 实现
```cpp
// sqlite3_open → WAL 模式 → CREATE TABLE IF NOT EXISTS
// Execute: sqlite3_exec + 返回 sqlite3_changes
// Query: sqlite3_prepare_v2 → 逐行 → SqlRow → vector
```

**Libs/Global/LogManager.cpp** — 日志文件名
```cpp
// gateway_%Y%m%d.log → signaling_%Y%m%d.log
```

### 新增清单

**Services/signaling/Config/server_config.json**
```json
{"Server":{"HttpPort":8080,"WsPort":8081,"StunPort":3478,
 "MaxConnections":10000,"WorkerThreads":4,"LogLevel":"debug"},
 "Database":{"Path":"./signaling.db"}}
```

**Services/signaling/main.cpp** — 信号处理 + Init + Start 阻塞
```cpp
// signal(SIGINT) → g_stop → app.Stop()
```

**Services/signaling/SigningApp.h/.cpp** — 主应用
```cpp
// Init(): 配置→日志→线程池→HTTP→STUN
// Start(): HTTP独立线程 + io_ctx.run()
// InitHttpRoutes(): register/heartbeat/devices/metrics 3个API
```

**Services/signaling/CMakeLists.txt**
```cmake
# add_executable → target_link(ds_thread ds_db ds_global ds_http)
# add_custom_command POST_BUILD: copy Config/ → 输出目录
```

## 步骤 2: SQLite + 设备注册 API — 修复记录

| 问题 | 根因 | 修复 |
|------|------|------|
| vcpkg find_package(SQLite3) 失败 | 包名不对 | `find_package(unofficial-sqlite3 CONFIG REQUIRED)` |
| curl → {"error":"db_unavailable"} | httplib 线程无 thread_local SqlExec | `GetCurrentThreadSqlExec()` 按需创建 |
| Instance() 类型推导失败 | shared_ptr 不能 auto* | `auto cfg = GlobalConfig::Instance()` |
| LogManager::Init 非静态 | 成员函数 | `LogManager::Instance()->Init()` |
| HttpServerMgr::GetServer 不存在 | API 不同 | 直接 `http_->Post(path, handler)` |
| unique_ptr 析构不完整类型 | 前向声明 | `#include "HttpServerMgr.h"` |
| file(GLOB) 不刷新新文件 | CMake 缓存 | 删 build 重配 Ninja |
| Debug/Release 混用 | VS 多配置生成器 | 换 Ninja 单配置 |

### API 验收

```bash
curl -X POST localhost:8080/api/register -d '{"id":"pc1","name":"办公室","local_ip":"192.168.1.1"}'
# → {"ok":true}

curl localhost:8080/api/devices
# → {"devices":[{"id":"pc1","name":"办公室电脑","online":true}],"count":1}
```

## 步骤 4: WebSocket 推送 — 实现

### WsSession.h/.cpp (~90行) — Boost.Beast WebSocket 会话

```
创建: WsSession::Create(tcp::socket) → Start() → DoAccept()
握手: ws_.async_accept() → DoRead() 循环
注册: 客户端发 {"type":"hello","device_id":"xxx"} → Register(WsSessionManager)
读写: async_read → OnMessage(JSON) → async_write
断线: async_read error → Broadcast device_offline → Remove
```

### WsSessionManager.h/.cpp (~90行) — 设备→会话映射

```cpp
class WsSessionManager (Singleton):
  Register(device_id, session)     // 新连接替换旧连接
  Remove(device_id)                 // 断开清理
  SendToDevice(device_id, json)    // 单播
  Broadcast(json)                  // 广播所有设备
  mutex_ 保护 sessions_ map
```

### 集成 SignalingApp

```cpp
// SignalingApp.h: ws_acceptor_(io_ctx_), InitWsAcceptor(), DoAcceptWs()
// Init:      InitWsAcceptor() → bind :8081 → DoAcceptWs()
// DoAccept:  async_accept → WsSession::Create(socket) → session->Start()
```

### Boost.Beast 版本兼容修复

| 问题 | 修复 |
|------|------|
| async_accept(ws_req_) 不兼容 Beast 1.88 | 纯握手 → 客户端发 hello 注册 |
| boost::json::string_view → std::string | 显式 data()+size() 构造 |
| websocket::stream 无 get_request() | 去掉 URL 参数方案 |

### 测试

```bash
python Tests/ws/test_ws_client.py pc1
# → {"type":"device_online","id":"pc1"}

# 开两个终端:
# 终端1: python Tests/ws/test_ws_client.py pc1  (继续监听)
# 终端2: python Tests/ws/test_ws_client.py pc2
# → 终端1 收到 {"type":"device_online","id":"pc2"}
```

### Websocket 协议

```
客户端 → 服务器:  {"type":"hello","device_id":"pc1"}
服务器 → 客户端:  {"type":"device_online","id":"pc1"}
服务器 → 所有设备: {"type":"device_offline","id":"pc1"}  (断开时)
服务器 → 目标设备: {"type":"offer","from":"ctrl","sdp":"..."}   (步骤5)
服务器 → 目标设备: {"type":"answer","from":"target","sdp":"..."} (步骤5)
服务器 → 目标设备: {"type":"ice","from":"ctrl","candidate":"..."} (步骤5)
```

## 步骤 5: 打洞信令交换 — 实现

### 实现

```
SignalingApp::InitHttpRoutes — 新增 3 个路由:
  POST /api/offer   ← 控制端发 → WS 转发到被控端
  POST /api/answer  ← 被控端回 → WS 转发到控制端
  POST /api/ice     ← 双向交换候选地址

实现: relaySignaling lambda (30行)
  → boost::json::parse(req.body) → from/to
  → WsSessionManager::Get().SendToDevice(to, req.body)  // JSON 原样转发
  → IncrementCounter("signaling_offer/answer/ice_total")
```

### 验收

```
两台机器:
  A: 注册 pc_a + WS 监听
  B: 注册 pc_b + WS 监听
  B: curl POST /api/offer -d {"from":"pc_b","to":"pc_a","sdp":"test"}
  → A 的 WS 收到 {"from":"pc_b","to":"pc_a","sdp":"test"}
  → B 断线时 A 收到 {"type":"device_offline","id":"pc_b"}
```

## 步骤 3: STUN 服务器 — 实现

### StunServer.h/.cpp (~150行)

```
协议: RFC 5389 Binding Request/Response
监听: UDP :3478
事件: async_receive_from → async_send_to

请求 (20字节):
  [0..1]  Type=0x0001 (Binding Request)
  [2..3]  Length=0
  [4..19] Transaction ID

响应 (32字节):
  [0..1]  Type=0x0101 (Binding Success Response)
  [2..3]  Length=12
  [4..19] Transaction ID (回显)
  [20..31] XOR-MAPPED-ADDRESS (type=0x0020, len=8)
           family=0x01, xport=port^0x2112, xaddr=addr^0x2112A42F
```

### 集成 SignalingApp

```cpp
// Init:  stun_ = std::make_unique<StunServer>(io_ctx_);
// Start: stun_->Start(cfg->GetStunPort());  // UDP :3478
// Stop:  stun_->Stop();
```

### 测试 (Tests/stun/test_stun_client.py)

```bash
python Tests/stun/test_stun_client.py
# → PASS xor_mapped: 127.0.0.1:58402
```

## 步骤 6: 客户端 SignalingClient — 实现 (~180行)

### SignalingClient.h/.cpp — Qt 原生网络（零新增依赖）

```
class SignalingClient : QObject {
    HTTP: QNetworkAccessManager
      POST /api/register, POST /api/heartbeat, GET /api/devices,
      POST /api/offer, POST /api/answer, POST /api/ice
    WS: QWebSocket
      ws://server:8081/ws → hello → OnSignaling/OnDeviceStatus
    回调: OnDeviceList, OnSignaling, OnDeviceStatus
}
```

### 集成 PlayerApp::Init()

```cpp
// 服务器 IP 自适应: 同机 127.0.0.1, 异机 192.168.31.142
signaling_->SetServer(server, 8080, 8081);
signaling_->RegisterDevice(QSysInfo::machineHostName(), ...);
signaling_->StartHeartbeat(device_id, 30000);
```

### 项目配置

```
Player.vcxproj: QtModules +websockets
                ClCompile +ClInclude: SignalingClient.h/.cpp
```

### 修复记录

| 问题 | 修复 |
|------|------|
| 两台同名 device_id 互踢 | QSysInfo::machineHostName() 区分 |
| QWebSocket connect 信号漏掉 | 所有 connect 移到 open() 前 |
| Qt 5.14 本机公网 IP UnsupportedSocketOperationError | DESKTOP-IASOGT4 用 127.0.0.1 |
| 转发消息缺 type 字段 | relaySignaling 补 obj["type"]=msg_type |

### 验收

```
服务器: SignalingServer + 两台 Player 各注册上线
curl POST /api/offer → 远程 Player 日志:
[Signal] 收到: QJsonObject({"from":"DESKTOP-IASOGT4","type":"offer","sdp":"test"})
```
```
